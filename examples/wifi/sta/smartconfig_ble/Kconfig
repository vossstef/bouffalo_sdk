mainmenu "SDK Configuration"

config BFLB_BLESYNC
    bool "Build BLE smartconfig synchronization"
    default n
    help
      Build the BLE synchronization service used by this example.

source "$BL_SDK_BASE/Kconfig"
